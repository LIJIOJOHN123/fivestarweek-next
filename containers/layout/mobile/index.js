import React from "react";
import Mobile from "./Mobile";

const MobileMenu = ({ themeValue }) => {
  return (
    <div>
      <Mobile themeValue={themeValue} />
    </div>
  );
};

export default MobileMenu;
